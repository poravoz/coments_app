app = "coments-app"

[build]
  builder = "dockerfile"

[http_service]
  internal_port = 3000
  force_https = true
  
  [[http_service.ports]]
    port = 80
    handlers = ["http"]
  
  [[http_service.ports]]
    port = 443
    handlers = ["tls", "http"]